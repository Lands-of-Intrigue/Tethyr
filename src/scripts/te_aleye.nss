void main()
{
    object oPC = GetEnteringObject();
    SendMessageToPC(oPC,"This dank cellar once served as the storage space for the inn above, which has been destroyed. Now that the fighting is over, remaining knights and mercenaries can be heard drinking and recanting the details of the battle.");
}
